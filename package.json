{
  "name": "tiktok-live-notifier",
  "version": "1.0.0",
  "description": "A monitoring tool that checks if a TikTok profile is live-streaming and sends notifications to Discord.",
  "main": "app/app.js",
  "scripts": {
    "start": "nodemon --exec ts-node app/app.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Weverton J. da Silva",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.3.1",
    "@types/nodemailer": "^6.4.8",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "@sentry/node": "^7.57.0",
    "dotenv": "^16.3.1",
    "sleep": "^6.3.0",
    "tiktok-live-connector": "^1.0.3"
  }
}
